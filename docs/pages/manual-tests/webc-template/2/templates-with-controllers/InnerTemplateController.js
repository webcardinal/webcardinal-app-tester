const { Controller } = WebCardinal.controllers;

class InnerTemplateController extends Controller {

    constructor(...props) {
        super(...props);
    }
}

export default InnerTemplateController;