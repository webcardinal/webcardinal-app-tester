import { HYDRATED_CLASS, initTest } from '../../utils';
import environment from '../../environments/1';

describe('psk-barcode-scanner', () => {
    beforeEach(async () => await initTest(environment));
})